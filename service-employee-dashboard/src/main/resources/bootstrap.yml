#konfigurasi service employee dashboard
spring:
   application:
      name: service-employee-dashboard
server:
   port: ${PORT:8081}
---
spring:
   cloud:
      config:
         uri: http://localhost:9090
security:
   basic:
      enable: false
management:
   security:
      enabled: false   
